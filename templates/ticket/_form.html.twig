{{ form_start(form) }}
    {{ form_label(form.title) }}
    {{ form_widget(form.title) }}
    <div class="mt-2">{{ form_label(form.message) }}</div>
    {{ form_widget(form.message) }}
    <button class="btn mt-3 mb-2" id="saveBtn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
